const crypto=require('crypto');const{safeStorage}=require('electron');class SecurityManager{constructor(config,logger){this.config=config;this.logger=logger}async initialize(){if(!this.config.data.deviceId){this.config.data.deviceId=crypto.randomUUID();this.config.data.encryptedDeviceSecret=this.encrypt(crypto.randomBytes(32).toString('base64url'));await this.config.save()}}encrypt(t){return safeStorage.isEncryptionAvailable()?safeStorage.encryptString(t).toString('base64'):Buffer.from(t).toString('base64')}decrypt(v){if(!v)return'';try{const b=Buffer.from(v,'base64');return safeStorage.isEncryptionAvailable()?safeStorage.decryptString(b):b.toString()}catch{return''}}deviceId(){return this.config.data.deviceId}deviceSecret(){return this.decrypt(this.config.data.encryptedDeviceSecret)}deviceToken(){return this.decrypt(this.config.data.encryptedDeviceToken)}async storeDeviceToken(t){this.config.data.encryptedDeviceToken=this.encrypt(t);this.config.data.paired=!!t;await this.config.save()}}module.exports={SecurityManager};
